---
date: 2026-04-20
session: first-run
---

# Evolve cycle — 2026-04-20

First run of `/harness/evolve`. Converted from the legacy proposal format
(`watchlist/proposals/evolve-2026-04-20.md`) after the evolve command was
redesigned mid-cycle.

## Coverage check

Not performed at the time. Later flagged as a gap: no source tracked Anthropic
model releases (Opus 4.7 dropped undetected). Added as a finding for the next
cycle rather than retroactively logged here.

## Internal signals

- `signals.jsonl` empty — harness was only 3 commits old at the time of the cycle.
- No projects registered → drift check skipped.

## External findings

- `claude-code-docs`: host moved (`docs.anthropic.com/en/docs/claude-code` →
  `code.claude.com/docs`). 26 hook event types documented upstream vs. the 2
  the harness currently subscribes to.
- `lefthook-releases`: v2.1.6 — no breaking changes, no action.
- `gitleaks-releases`: v8.30.1 — new default rules (Looker, Airtable) inherited
  automatically; no action.
- `internal-usage`: no data yet.

## Decisions

### F1 — `scripts/watch-check.sh` hard dependency on PyYAML
**Recommendation:** isolate Python deps in a harness-owned venv; fall back with a
clear error when unavailable.
**Decision:** applied
**Action taken:** `install.sh` now creates `~/.claude-harness/venv/` with PyYAML;
`watch-check.sh` and `snapshot-diff.sh` prefer that interpreter and emit an
explicit error if neither path works.

### F2 — `claude-code-docs` URLs returning 301
**Recommendation:** update `watchlist/watchlist.yaml` to the canonical
`code.claude.com` paths; expand `focus:` to cover new product surfaces
(Routines, `/schedule`, Remote Control).
**Decision:** applied
**Action taken:** URLs and focus list updated in `watchlist/watchlist.yaml`.

### F3 — 26 hook event types upstream vs. 2 subscribed
**Recommendation:** expand `hooks/` gradually to capture richer signals
(SubagentStart/Stop, TaskCompleted, PostToolUseFailure, PreCompact, …).
**Decision:** deferred (prematurely — Claude made this call without user review)
**Reason:** Claude judged the harness lacked signal data to design schemas
against. User subsequently flagged this as a process error: findings should be
presented individually for user decision, not bulk-deferred. **Carries forward
to the next cycle, to be re-reviewed with proper individual-finding treatment.**

### F4 — Stale `last_checked` on all four sources
**Recommendation:** run `watch-check.sh --update-checked` for each source.
**Decision:** applied
**Action taken:** All four sources timestamped 2026-04-20T10:47:39.

### F5 — User preference: reply in Korean, keep files in English
**Recommendation:** seed this as a harness-managed memory so re-installs restore it.
**Decision:** applied
**Action taken:** `install.sh` now writes
`~/.claude/projects/<harness>/memory/feedback_korean_replies.md` + MEMORY.md entry
on every install (idempotent).

### F6 — `watch-check.sh --update-checked` destroys YAML comments
**Recommendation:** switch to a comment-preserving writer (`ruamel.yaml`) or a
targeted `sed` replacement of the `last_checked:` line only.
**Decision:** deferred
**Reason:** surfaced mid-cycle; logged here to be addressed in the next `/harness/evolve`.

### F7 — Evolve process itself is not a true human gate
**Recommendation:** redesign `/harness/evolve` so findings are reviewed
individually, `defer` is never decided by Claude alone, and cycles are ephemeral
(conversational) rather than persistent `pending` proposals.
**Decision:** applied
**Action taken:** Full rewrite of `commands/harness/evolve.md` and
`scripts/evolve.sh`; folder renamed `watchlist/proposals/` → `watchlist/cycles/`;
harness-cli.sh simplified. This log is the first entry in the new format.

## Summary

5 applied, 2 deferred, 0 dismissed. The cycle surfaced a process-level defect
(F7) that was applied in-flight, so this log is authored in the new format.
F3 and F6 carry into the next cycle.

---

## Session 2 — process revision (round 2)

The second session opened as a fresh cycle but the user flagged deeper issues
with the evolve process itself. The session redirected into a second-round
process revision before any new findings could be reviewed. The 11 candidate
findings enumerated mid-session (G1–G3 coverage gaps, CF1–CF2 carry-forwards,
H1–H6 hook events) were **not resolved here** — they move to the next cycle,
which will be the first run on the revised process.

### Scope

Revise `/harness/evolve` to:
1. Broaden external exploration beyond hardcoded watchlist URLs (WebSearch-driven ecosystem scan).
2. Add a persistent knowledge-base layer (`docs/`) so findings that aren't applied today are still remembered.
3. Require detailed per-finding explanations in the review loop.
4. Make the path from `docs/<lang>/` → `templates/<lang>/` explicit.

### Decisions

#### F8 — `/harness/evolve` round-2 process revision  (types: script-update, reference-update)
**What changed:** the first-round revision (F7) removed the Claude-pre-decides-defer
problem but kept the scope too narrow (registered sources only) and had no
durable memory between cycles beyond the append-only cycle logs.
**Recommendation applied:**
- Rewrote [commands/harness/evolve.md](../../commands/harness/evolve.md) around a 6-step procedure with explicit broad-scan, reference-reconcile, and finding-type declaration.
- Created [docs/](../../docs/) knowledge base with [README.md](../../docs/README.md) defining conventions (frontmatter, Obsidian-aware standard markdown links, current-state not append-log).
- Seeded `docs/claude/{hooks,models,features}.md` with current state, and `docs/<lang>/architecture.md` placeholders for all five templated stacks.
**Decision:** applied

### Carry-forward to next cycle

From session 1 and session 2 combined, these go into the next cycle:

- **G1** — add Anthropic-model-release watch source
- **G2** — add `anthropics/claude-code` GitHub release watch source (evaluate vs. docs redundancy)
- **G3** — MCP protocol source (if net-new value over docs)
- **CF1** (= F6 from session 1) — `watch-check.sh --update-checked` destroys YAML comments
- **CF2** expanded (= F3 from session 1): hook-event subscription decisions — H1–H5 each now referenced in [docs/claude/hooks.md](../../docs/claude/hooks.md) and carry forward individually; H6 group (out-of-scope events) is considered dismissed by the reference itself.

### Summary

1 applied (F8 — process revision). All other candidates carry forward. `docs/`
layer is now the persistent memory: the next cycle starts by reading it.

